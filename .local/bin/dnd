#!/usr/bin/env bash
case `dunstctl is-paused` in
    true)
	rm -f /tmp/dnd.lock
        dunstctl set-paused false
        ;;
    false)
	touch /tmp/dnd.lock
        (dunstctl close && dunstctl set-paused true) &
        ;;
esac
